{
  "name": "spleen-mongodb",
  "longName": "spleen-mongodb",
  "description": "Convert spleen filters into MongoDB query DSL.",
  "version": "1.0.0",
  "dependencies": {
    "elv": "^2.0.0",
    "spleen": "^1.3.0"
  },
  "devDependencies": {
    "chai": "~3.5.0",
    "eslint": "^4.9.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0",
    "mocha": "^3.2.0",
    "nyc": "^10.2.0"
  },
  "keywords": [
    "conditional",
    "filter",
    "graph",
    "query",
    "dynamic",
    "if",
    "where",
    "search",
    "api",
    "input",
    "param",
    "mongodb",
    "mongo",
    "nosql"
  ],
  "license": "MIT",
  "main": "./lib",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/dsfields/spleen-mongodb-node"
  },
  "scripts": {
    "build": "npm test && npm run lint",
    "lint": "eslint ./*",
    "test": "NODE_ENV=test nyc --reporter=lcov  --reporter=text mocha --recursive ./test/unit"
  }
}
